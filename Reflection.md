Driver : Victory
Navigator: Nafi


Output" Grade B
Reflection Questions (Memory Tracing Table)

1. How does fall-through logic in switch help display cumulative features?

Fall-through logic lets the code keep running into the next case if there’s no break. This is helpful when one option includes all the features of the ones before it. It makes it easy for us to show all the features together without writing extra code.


2. What are the risks of forgetting a break statement?

If we forget to put a break, the program will keep going to the next case even when it shouldn’t. This can make the program show the wrong results or print more than what was supposed to appear.


3. How did memory tracing help you understand variable updates?

Memory tracing helped us see how the values of variables changed while the program was running. It showed us where each variable started, when it changed, and what its final value was, which made the program easier to understand.


4. How would you modify the flooring program to allow multiple selections?

We would add a loop so the user could pick more than one flooring option before quitting. We’d also make a total cost variable to add up all the prices. The loop could stop when the user enters a number like 0 to finish choosing.

------------------------------------------------------------------------------------------------------------------------------------------------------------------------

1. How did n-way selection help simplify the logic in Task 3?

Using an if-else-if chain made it easier to handle all the different grade ranges in one place. Each part of the code checked one condition, so we didn’t have to write a bunch of separate if statements. It kept the program short, organized, and easy to understand.

2. What challenges did you face when nesting conditions in Task 4?

The hardest part was keeping track of which if went with which else. When we nested conditions, it was easy to mix up braces or indentation. We had to stay careful so that each message matched the right situation for age and passport.


3. How did memory tracing help you understand control flow?

Memory tracing showed us how the program moved from one condition to another. It helped us see which parts of the code were actually running and how the variables changed as the program went on. That made it easier to follow the program’s flow.


4. What strategies did you use to test and verify your logic?

We tested the code by trying lots of different inputs to see what would happen. For example, we entered different grades and ages, with and without a passport. Doing that helped us check that each part of the program worked the way it was supposed to.

